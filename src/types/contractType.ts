export interface ContractParticipant {
  id: number;
  name: string;
}

export interface ContractCar {
  id: number;
  model: string;
}

export interface Meeting {
  date: string;
  alarms: string[];
}
